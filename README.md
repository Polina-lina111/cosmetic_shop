## Основні сутності

### Category
Клас, який описує категорію косметичних товарів.

Приклади категорій:
- догляд за шкірою;
- макіяж;
- парфуми.

### Product
Клас, який описує косметичний товар.

Товар містить:
- назву;
- бренд;
- ціну;
- кількість;
- категорію.

---

## Зв'язок між сутностями

Одна категорія може містити багато товарів.

Приклад зв'язку: Category -> Product

---

## Реалізовані методи

### show_info()
Виводить інформацію про товар.

### update_price()
Змінює ціну товару.

### sell_product()
Зменшує кількість товару після продажу.

## Маршрути проєкту

### Головна сторінка
http://127.0.0.1:8000/

### Product parameter
http://127.0.0.1:8000/product/5/

### Query string
http://127.0.0.1:8000/search/?brand=Nivea

### Redirect
http://127.0.0.1:8000/shop/

---

## Templates та detail view

У проєкті реалізовано сторінку detail для товару косметичного магазину.

### Реалізовано:
- detail view з параметром pk;
- передача context у template;
- використання template filters;
- цикл for для пов’язаних товарів;
- template inheritance через base.html.

### Використані template filters

#### upper
Назва товару виводиться великими літерами.

#### truncatewords:5
Опис товару скорочується до 5 слів.

#### date:"d.m.Y"
Дата відображається у форматі день.місяць.рік.

### Пов’язані товари

На сторінці detail виводиться список пов’язаних товарів:
- Lipstick
- Serum
- Face Mask

Якщо список порожній — виводиться:
"Нічого немає"

---

## Template inheritance та static files

У проєкті реалізовано:
- базовий шаблон `base.html`;
- успадкування шаблонів через `{% extends %}`;
- блоки `{% block content %}`, `{% block extra_css %}`, `{% block extra_js %}`;
- підключення static files через `{% static %}`;
- окремий CSS-файл `style.css`;
- окремий JavaScript-файл `main.js`;
- підключення зображення косметики;
- використання `include` для картки товару.

---

## Django Models

У проєкті створена модель `Product` для збереження інформації про косметичні товари.

### Поля моделі
- `title` — назва товару;
- `brand` — бренд;
- `description` — опис товару;
- `price` — ціна;
- `quantity` — кількість товару на складі.

### Реалізовано
- створення Django-моделі через `models.Model`;
- виконання `makemigrations` і `migrate`;
- реєстрація моделі в Django Admin;
- створення тестових записів через admin panel.

### Приклади товарів
- Nivea Cream
- Matte Lipstick
- Face Serum

---

## ORM та зв'язки моделей

У проєкті реалізовано:

- сторінку списку товарів через ORM;
- сторінку деталей товару через `pk`;
- використання `get_object_or_404`;
- зв'язок між моделями `Product` і `Category`;
- вивід даних із бази даних у templates;
- перехід між сторінками через посилання.

### ORM-запити

```python
Product.objects.all()


---

## CBV, форми та validation

У проєкті додано:

### About page через TemplateView
Сторінка `about/` працює через CBV:
`TemplateView.as_view()`.

### Форма відгуку
Реалізована Django-форма для надсилання відгуку про товар.

Форма містить:
- ім’я користувача;
- текст відгуку;
- рейтинг товару.

### Validation форми

Перевіряється:
- мінімальна довжина відгуку;
- коректність рейтингу.

### Нові маршрути

About page:
http://127.0.0.1:8000/about/

Review form:
http://127.0.0.1:8000/review/